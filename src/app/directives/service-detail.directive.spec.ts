import { ServiceDetailDirective } from './service-detail.directive';

describe('ServiceDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ServiceDetailDirective();
    expect(directive).toBeTruthy();
  });
});
